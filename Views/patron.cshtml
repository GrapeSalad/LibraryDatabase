@{
  Layout="Shared/_Layout.cshtml";
}
<br>
<h2>Books:</h2>
@if (Model.Count != 0)
{
  <ul>
    @foreach (var book in @Model)
    {
      <li><a href='/book/@book.GetId()'>@book.GetTitle()</a></li>
    }
  </ul>
}
else
{
  <p>AINT NOTHIN</p>
}
